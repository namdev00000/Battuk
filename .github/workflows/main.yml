name: Build Battuk Phase 2 Debug APK

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Make Gradle wrapper executable
        run: chmod +x Battuk-Phase2-MVP/BattukPhase1/gradlew

      - name: Build debug APK
        working-directory: Battuk-Phase2-MVP/BattukPhase1
        run: ./gradlew :app:assembleDebug --stacktrace

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: battuk-phase2-debug-apk
          path: Battuk-Phase2-MVP/BattukPhase1/app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
